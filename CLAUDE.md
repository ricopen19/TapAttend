# TapAttend

## GASデプロイ時の注意（ハマりどころ）

過去に「コードを直したのにGAS上のUIに反映されない」という事故が起きた。原因はエディタのキャッシュではなく、**`gas/index.html` を再ビルドせずに（または `npm run build` など別コマンドと混同して）古い内容のままGASに貼り付けていた**こと。タブを閉じて開き直しても、ディスク上のファイル自体が古ければ意味がない。

`Code.gs` を手動編集した場合も、ビルド漏れ・貼り付け忘れという同種の事故は起こりうる。

**対策**: GASへの反映は必ず `npm run copy:gas` を使う。ビルドを含めて1コマンドで実行され、`Code.gs`→貼り付け待ち→`index.html`→貼り付け待ち、の順に案内される。

```bash
npm run copy:gas
```

`npm run copy:gas` は2ファイルを順番に貼り付けてもらうための対話式スクリプト（Enter待ちが2回ある）。Claude（自分）が非対話でEnterを流し込んで実行すると、ユーザーが貼り付ける前に次のファイルでクリップボードが上書きされ、Code.gsが失われる（実際に発生した）。**Claudeは代理実行せず、コマンドだけ案内してユーザー自身に実行してもらう。**

デプロイ手順の詳細は `gas/README.md` を参照。
