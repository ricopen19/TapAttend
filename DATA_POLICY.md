# データの取り扱いについて

TapAttend で入力されたデータ（出欠状況・生徒メモなど）がどのように扱われるかを説明します。

## データの保存場所

入力されたデータは、**Google スプレッドシート**（出欠データ用に自動作成される専用ファイル）に保存されます。

- 名簿（出席番号・氏名）は別途用意された名簿マスタのスプレッドシートから読み取ります（TapAttend からは編集できません）
- 出欠データ・生徒メモは、TapAttend 用の専用スプレッドシートに保存されます
- どちらのスプレッドシートも、デプロイした Google Workspace アカウントの Google ドライブ上にあります
- データの実体は Google のインフラ上にあり、端末やブラウザには保存されません（同じ組織アカウントでログインすればどの端末からでも同じデータが見えます）

## 通信について

- TapAttend は Google Apps Script（GAS）の Web App として動作します
- アクセスには組織の Google アカウントでのログインが必要です（デプロイ設定で「組織内のみ」に制限）
- 出欠の入力・表示のたびに、ブラウザと Google のサーバー（Apps Script / スプレッドシート）の間で通信が発生します

## アクセス範囲

- 現状、クラス一覧・出欠データは組織内の全教員から閲覧・編集可能です（担任限定のアクセス制御は未実装）
- 担任以外が誤って他クラスのデータを編集しないよう、運用上の注意喚起を行ってください

## ログについて

- Google Workspace の管理コンソールで、アクセスログ（誰がいつアクセスしたか）が記録される場合があります。詳細は組織の Google Workspace 管理者にご確認ください

## データの消失リスク

- スプレッドシートを直接誤って削除・上書きした場合、データが失われる可能性があります
- Google スプレッドシートは変更履歴（バージョン履歴）を保持しているため、誤操作からの復旧がある程度可能です
- 定期的に JSON バックアップ（読み取り専用のスナップショット出力）を取ることを推奨します

## 個人情報の取り扱いに関する補足

生徒の個人情報の取り扱いが気になる場合、以下の運用を推奨します：

- **イニシャルで登録する**（例：「Y・T」「S・H」）※名簿マスタ側での対応が必要
- 出欠データ用スプレッドシートの共有範囲を必要最小限に保つ

## まとめ

| 項目 | 内容 |
|---|---|
| データ保存先 | Google スプレッドシート（GAS 経由） |
| アクセス範囲 | 組織内の Google アカウントでログインした教員 |
| オフライン動作 | 非対応（通信が必要） |
